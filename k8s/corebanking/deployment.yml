apiVersion: apps/v1
kind: Deployment
metadata:
  name: corebanking
  namespace: banking-platform
  labels:
    app: corebanking
spec:
  replicas: 2
  selector:
    matchLabels:
      app: corebanking
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: corebanking
    spec:
      containers:
        - name: corebanking
          # Image tag is updated by CI/CD pipeline via kubectl set image
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/banking-platform/corebanking:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: banking-platform-config
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: banking-platform-secrets
                  key: DB_URL
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: banking-platform-secrets
                  key: DB_USERNAME
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: banking-platform-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: banking-platform-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: corebanking
  namespace: banking-platform
spec:
  selector:
    app: corebanking
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
